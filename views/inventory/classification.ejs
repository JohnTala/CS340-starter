<main>
  <h1><%= title %></h1>

  <% if (message) { %>
    <p class="flash-success"><%= message %></p>
  <% } %>

  <!-- Inventory Grid -->
  <% if (grid) { %>
    <%- grid %>
  <% } else { %>
    <p class="notice">No vehicles available for this classification.</p>
  <% } %>
</main>
